import React from 'react';
import NavLinkWrapper from '../molecules/NavLinkWrapper'



const NavigationList = ({petname,heading,text,petImage, ...props}) => {
  return (
    <ul>
        <NavLinkWrapper />
    </ul>
    );
}

export default NavigationList;